# Deprecated

This code was migrated into `braingeneers.utils.memoize_s3`.

If you're not already using `braingeneers`, note that this library was written specifically for our research group's S3-like (Ceph) storage, and has never been tested anywhere else.
